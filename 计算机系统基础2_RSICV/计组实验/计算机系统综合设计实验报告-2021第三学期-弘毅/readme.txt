1、所属信息：
	WHU  计算机科学与技术 2019年7月8日；

2、由于顶层文件夹名为中文，所以不能直接打开运行，需要将modelsim或者ise的项目移至可运行位置，所有所属文件均在相应文件夹内，如果无法直接运行，可新建项目直接导入；

3、本项目主要包含实现了37条指令的单周期非流水线CPU和流水线CPU，其中单周期非流水线CPU中modelsim项目文件夹支持实现lw，sw，lb，lh，lhu，lbu和sb，sh，但对应的ise项目文件夹由于仿真软件限制，仅实现了lw，sw。对于流水线CPU中重点在于冒险和分支预测，modelsim和ise项目文件夹中都仅支持lw，sw指令，对于lb，lh，lhu，lbu和sb，sh指令只需要按照单周期非流水线CPU格式替换modelsim中dm部件和修改sccomp中的dm模块接口即可，原理与较为简单，不再展示。

4、modelsim仿真测试代码对于单周期非流水线CPU主要测试37条完整的指令，对于流水线CPU主要针对冒险和分支预测。

4、如有问题，请联系作者，邮箱：jeremyhua@foxmail.com
	